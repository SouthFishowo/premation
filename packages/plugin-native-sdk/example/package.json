{
  "name": "motion-example-native-addon",
  "version": "1.0.0",
  "private": true,
  "description": "Example Premation native addon. Built by hand; never by the editor's build or CI.",
  "scripts": {
    "build": "node-gyp rebuild",
    "build:electron": "cmake-js compile --runtime electron --runtime-version $ELECTRON_VERSION"
  },
  "devDependencies": {
    "node-addon-api": "^8.0.0"
  }
}
